# JavaScript-Fibonacci-
var yourself = {
    fibonacci : function(n) {
        if (n === 0 || n === 1)
        return n;
    {
            return fibonacci(n - 1) + fibonacci(n - 2);
        }
        
        if (n === 1) {
            return 1;
        }
    }
};

// Still need to define fibonacci //
